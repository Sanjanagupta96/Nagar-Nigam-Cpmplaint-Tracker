db.url=jdbc:mysql://localhost:3306/nagar_nigam_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.username=root
db.password=root

# Where uploaded attachments will be stored on the server
# For Jetty (mvn jetty:run) / portability, keep it relative to this project
uploads.dir=./nagar-nigam-uploads
